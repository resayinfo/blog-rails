<h3 class="commenter"><%= comment.commenter %></h3>
<p class="commented">created <%= comment.created_at.strftime('%Z, %b %-d, %Y, %l:%M %p') %></p>

<p class="comments-lefty"><%= comment.body %></p>

<% if can? :manage, Comment %>
  <p class="comments-lefty">
    <%= link_to 'Delete', [comment.article, comment], class: 'btn btn-danger',
                 method: :delete,
                 data: { confirm: 'Are you sure?' } %>
  </p>
<% end %>
