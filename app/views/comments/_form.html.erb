<%= form_for([@article, @article.comments.build]) do |f| %>

  <p>
    <%= f.text_area :body, class: "summernote", placeholder: '500,000 characters max' %>
  </p>
  <p>
    <%= f.submit class: 'btn btn-default' %>
  </p>

<% end %>
